#include <stdio.h>

// typecasting converting one datatype in another
// syntax
// (type) value;

int main()
{
    int a = 3;
    float b = (float)54 / 5;
    printf("the value of a is %f\n", b);
    return 0;
}
